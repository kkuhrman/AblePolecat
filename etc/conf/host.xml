<?xml version='1.0' standalone='yes'?>
<host>
  <databases>
    <!-- 
    More than one application database can be defined in server conf file. However, only ONE
    application database can be active per server mode. If 'mode' attribute is empty, polecat 
    will assume any mode. Otherwise, database is defined for given mode only. The 'use' attribute
    indicates that the database should be loaded for the respective server mode. Polecat will 
    scan database definitions until it finds one suitable for the current server mode where the
    'use' attribute is set. 
    -->
    <database id="core" name="polecat" mode="server" use="1">
      <dsn>mysql://user:pass@localhost/polecat</dsn>
    </database>
  </databases>
</host>